const express = require('express');
const router = express.Router();

// Placeholder AI routes
router.get('/', (req, res) => {
  res.json({ message: 'AI endpoints - not implemented yet' });
});

module.exports = router;