const express = require('express');
const router = express.Router();

// Placeholder learning routes
router.get('/', (req, res) => {
  res.json({ message: 'Learning endpoints - not implemented yet' });
});

module.exports = router;