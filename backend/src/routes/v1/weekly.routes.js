const express = require('express');
const router = express.Router();

// Placeholder weekly routes
router.get('/', (req, res) => {
  res.json({ message: 'Weekly endpoints - not implemented yet' });
});

module.exports = router;