const express = require('express');
const router = express.Router();

// Placeholder budget routes
router.get('/', (req, res) => {
  res.json({ message: 'Budget endpoints - not implemented yet' });
});

module.exports = router;